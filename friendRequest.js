// Constructor for a friend request object
class FriendRequest {
    constructor(message, status, date) {
        this.message = message;
        this.status = status;
        this.date = date;
    }
}

export default FriendRequest;
